<?xml version="1.0" encoding="UTF-8" ?>   
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.tsjx.auditHis.dao.impl.AuditHisDaoImpl">

	<!-- 插入实体 -->
	<insert id="insert" parameterType="cn.com.tsjx.auditHis.entity.AuditHis">
		<![CDATA[
		INSERT INTO TSJX_AUDIT_HIS (
		   	ID,
		   	TYPE,
		   	CREATE_ID,
		   	CREATE_BY,
		   	CREATE_TIME,
		   	REMARK,
		   	RESULT
		) VALUES (
		   	#{id,jdbcType=INTEGER},
		   	#{type,jdbcType=VARCHAR},
		   	#{createId,jdbcType=INTEGER},
		   	#{createBy,jdbcType=VARCHAR},
		   	#{createTime,jdbcType=TIMESTAMP},
		   	#{remark,jdbcType=VARCHAR},
		   	#{result,jdbcType=VARCHAR}
		)
		]]>
		<selectKey databaseId="mysql" keyProperty="id" order="AFTER" resultType="long">
			SELECT LAST_INSERT_ID() AS ID
		</selectKey>
		<selectKey databaseId="oracle" keyProperty="id" order="BEFORE" resultType="long">
			<![CDATA[
				SELECT SEQ_TSJX_AUDIT_HIS.nextval as id FROM dual
			]]>
		</selectKey>
	</insert>

	<!-- 更新实体 -->
	<update id="update" parameterType="cn.com.tsjx.auditHis.entity.AuditHis">
		UPDATE TSJX_AUDIT_HIS
		<set>
			<if test="type != null">
				TYPE = #{type,jdbcType=VARCHAR},
			</if>
			<if test="createId != null">
				CREATE_ID = #{createId,jdbcType=INTEGER},
			</if>
			<if test="createBy != null">
				CREATE_BY = #{createBy,jdbcType=VARCHAR},
			</if>
			<if test="createTime != null">
				CREATE_TIME = #{createTime,jdbcType=TIMESTAMP},
			</if>
			<if test="remark != null">
				REMARK = #{remark,jdbcType=VARCHAR},
			</if>
			<if test="result != null">
				RESULT = #{result,jdbcType=VARCHAR},
			</if>
		</set>
		WHERE ID = #{id,jdbcType=INTEGER}
	</update>

	<!-- 删除实体 -->
	<update id="delete" parameterType="cn.com.tsjx.auditHis.entity.AuditHis">
		<![CDATA[
		DELETE FROM TSJX_AUDIT_HIS WHERE ID = #{id,jdbcType=INTEGER}
		]]>
	</update>

	<!-- 查询字段 -->
	<sql id="selectFieldSQL">
		<![CDATA[
		SELECT 
		       ID id,
		       TYPE type,
		       CREATE_ID createId,
		       CREATE_BY createBy,
		       CREATE_TIME createTime,
		       REMARK remark,
		       RESULT result
		]]>
	</sql>

	<!-- 查询 -->
	<select id="get" parameterType="map" resultType="cn.com.tsjx.auditHis.entity.AuditHis">
		<include refid="selectFieldSQL" />
		<![CDATA[
		  FROM TSJX_AUDIT_HIS
		 WHERE ID = #{id,jdbcType=INTEGER}
		]]>
	</select>

	<!-- 查询 -->
	<select id="find" parameterType="cn.com.tsjx.auditHis.entity.AuditHis" resultType="cn.com.tsjx.auditHis.entity.AuditHis">
		<include refid="selectFieldSQL" />
		<![CDATA[
		  FROM TSJX_AUDIT_HIS
		]]>
		<where>
			<if test="id != null">
				AND ID = #{id,jdbcType=INTEGER}
			</if>
			<if test="type != null and type != ''">
				AND TYPE = #{type,jdbcType=VARCHAR}
			</if>
			<if test="createId != null">
				AND CREATE_ID = #{createId,jdbcType=INTEGER}
			</if>
			<if test="remark != null and remark != ''">
				AND REMARK = #{remark,jdbcType=VARCHAR}
			</if>
			<if test="result != null and result != ''">
				AND RESULT = #{result,jdbcType=VARCHAR}
			</if>
		</where>
	</select>

	<!-- 查询所有 -->
	<select id="findAll" parameterType="map" resultType="cn.com.tsjx.auditHis.entity.AuditHis">
		<include refid="selectFieldSQL" />
		<![CDATA[
		  FROM TSJX_AUDIT_HIS
		]]>
	</select>

	<!-- 分页查询（条件） -->
	<sql id="page_condition">
		<where>
				<if test="entity.type != null and entity.type != ''">
					AND TYPE = #{entity.type,jdbcType=VARCHAR}
				</if>
				<if test="entity.createId != null">
					AND CREATE_ID = #{entity.createId,jdbcType=INTEGER}
				</if>
				<if test="entity.remark != null and entity.remark != ''">
					AND REMARK = #{entity.remark,jdbcType=VARCHAR}
				</if>
				<if test="entity.result != null and entity.result != ''">
					AND RESULT = #{entity.result,jdbcType=VARCHAR}
				</if>
		</where>
	</sql>

	<!-- 分页查询（计数） -->
	<select id="page_count" parameterType="map" resultType="int">
		<![CDATA[
		SELECT count(*)
		  FROM TSJX_AUDIT_HIS
		]]>
		<include refid="page_condition" />
	</select>

	<!-- 分页查询（数据） -->
	<select id="page_datas" parameterType="map" resultType="cn.com.tsjx.auditHis.entity.AuditHis">
		<include refid="mybatis.pageStartSQL" />
		<include refid="selectFieldSQL" />
		<![CDATA[
		  FROM TSJX_AUDIT_HIS
		]]>
		<include refid="page_condition" />
		<include refid="mybatis.pageEndSQL" />
	</select>

</mapper>   
